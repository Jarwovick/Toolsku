<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Lihat semua app -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Ukuran cache -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Kill background process -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!-- Daftar running apps -->
    <uses-permission android:name="android.permission.GET_TASKS" />

    <!-- Kunci rotasi saat otomasi -->
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ToolskuApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Toolsku"
        tools:targetApi="28">

        <!-- ============================================ -->
        <!-- HOME -->
        <!-- ============================================ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Toolsku">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================ -->
        <!-- ONBOARDING -->
        <!-- ============================================ -->
        <activity
            android:name=".onboarding.PermissionActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- ============================================ -->
        <!-- DIMMER -->
        <!-- ============================================ -->
        <activity
            android:name=".dimmer.DimmerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <service
            android:name=".dimmer.DimmerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="screen_dimmer_overlay" />
        </service>

        <!-- ============================================ -->
        <!-- BOOT MENU -->
        <!-- ============================================ -->
        <activity
            android:name=".boot.BootActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- ============================================ -->
        <!-- CLEANER -->
        <!-- ============================================ -->
        <activity
            android:name=".cleaner.CleanerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- ============================================ -->
        <!-- KILLER -->
        <!-- ============================================ -->
        <activity
            android:name=".killer.KillerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- ============================================ -->
        <!-- ACCESSIBILITY SERVICE -->
        <!-- ============================================ -->
        <service
            android:name=".automation.AutomationAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
